name: C/C++ CI

on:
  push:
    branches: [ "builds-branch" ]
  pull_request:
    branches: [ "builds-branch" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: configure
      run: ./configure.ac
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
